def getOperator(s):
    a_op=[]
    a_remain=[]
    
    for i in range(0,len(s)):
        if s[i] == "+":
            a_op += s[i]
        elif s[i] == "-":
            a_op += s[i]
        else:
            i=
            break
    for a in range(k,len(s)):
               
    return a_op
    return a_remain
